import React from 'react';
import { Button } from 'react-bootstrap';

const Home = () => {
    return (
        <div>
            <Button variant="primary">Add User</Button>
        </div>
    );
};

export default Home;